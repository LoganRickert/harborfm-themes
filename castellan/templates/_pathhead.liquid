<header class="gatehead">
  <div class="gatehead__inner">
    <a class="gatehead__brand" href="{{ urls.home }}">
      <span class="gatehead__seal" aria-hidden="true"></span>
      <span class="gatehead__show">{{ podcast.title | escape }}</span>
    </a>
    <h1 class="gatehead__title display">{{ title | escape }}</h1>
  </div>
</header>
